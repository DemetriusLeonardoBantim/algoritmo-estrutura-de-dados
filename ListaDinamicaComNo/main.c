#include <stdio.h>
#include <stdlib.h>
#include "ListaDinamicaComNo.h"

int main()
{
  Lista *li;

  li = cria_lista();
  libera_lista(li);

  int tamanho = tamanho_lista(li)

      //insere
      int insere = insere_list_inicio(li, dados_aluno);
  int insere_lista_final(li, dados_aluno);

  //remove
  int remove_final = remove_lista_final(li);
}