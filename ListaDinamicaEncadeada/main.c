#include <stdio.h>
#include <stdlib.h>
#include "ListaDinEncad.h"

int main(){
  Lista *li

  li = cria_lista()

  int x = lista_vazia(Lista* li);
  if(lista_vazia(li));


  //consulta
  int x = consulta_lista_pos(li, posicao, &dados_aluno);
  int y = consulta_lista_mat(li, posicao, &dados_aluno);
}


