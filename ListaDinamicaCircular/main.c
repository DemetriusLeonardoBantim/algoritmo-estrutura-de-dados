#include <stdio.h>
#include <stdlib.h>
#include "ListaEncadeadaCircular.h"



int main(){

  Lista *li; 


  //Para liberar a lista
  libera_lista(li);
  
  int x = lista_cheia(li);
  if(lista_cheia(li));

  int y = lista_vazia(li);
  if(lista_vazia(li));
  
  return 0;
}